# mailcheck
IMAP inbox checker — list recent/unseen emails from CLI.
```bash
python mailcheck.py --host imap.gmail.com --user me@gmail.com --unseen
MAIL_PASSWORD=xxx python mailcheck.py --host imap.mail.me.com --user me@icloud.com -n 20
```
## Zero dependencies. Python 3.6+.
